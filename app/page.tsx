import { opens } from '../app/layout'

export default function Home() {
  return (
    <main className="p-5">
      <h1 className={`${opens.className} text-5xl font-medium`}>fodase mundo</h1>
    </main>
  );
}
